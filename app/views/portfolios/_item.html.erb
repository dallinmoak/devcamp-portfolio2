  <h3><%=link_to item.title, portfolio_show_path(item.id) %></h3>
  <h4><%= item.subtitle %></h4>
  <p><%= item.body %></p>
  <%= image_tag item.thumb_image if !item.thumb_image.nil? %>
  <%= link_to "edit", edit_portfolio_path(item.id) %>
  <%= link_to 'Delete Item', portfolio_path(item.id), method: :delete, data: {confirm: 'Are you sure?'} %>
